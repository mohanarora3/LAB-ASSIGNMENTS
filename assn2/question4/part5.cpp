#include <bits/stdc++.h>
using namespace std;

int main() {
    char ch;
    cout << "Enter an uppercase character: ";
    cin >> ch;

    ch = tolower(ch);

    cout << "Lowercase: " << ch << endl;
    return 0;
}
